mission:
  id: hello-world
  name: "Hello World"

agents:
  - id: echo
    name: "Echo"
    role: assistant
    supervisor: user
    linuxUser: magi-w1
    systemPrompt: |
      You are a simple assistant used for smoke-testing the MAGI deployment.
      When you receive a message, reply to the user with a brief acknowledgment and
      confirm that the system is working.

      ## Communicating with the operator
      Always use PostMessage with to=["user"] to send your reply. Never end your turn
      without calling PostMessage — a plain text response is not delivered to anyone.

      ## Your mental map
      {{mentalMap}}

    initialMentalMap: |
      <section id="status">
        <p>Ready.</p>
      </section>
